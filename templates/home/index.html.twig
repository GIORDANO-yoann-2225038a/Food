{% extends 'base.html.twig' %}
{% block title %}Notre Restaurant{% endblock %}

{% block body %}
    <div class="bg-gray-100 min-h-screen">
        <!-- Header -->
        <header class="bg-white shadow-md">
            <div class="container mx-auto px-6 py-4 flex justify-between items-center">
                <h1 class="text-3xl font-bold text-gray-800">Le Gourmet</h1>
                <nav>
                    <ul class="flex space-x-6">
                        <li><a href="{{ path('app_home') }}" class="text-gray-700 hover:text-gray-900">Accueil</a></li>
                        <li><a href="{{ path('shopping_list') }}" class="text-gray-700 hover:text-gray-900">Liste de courses</a></li>
                        <form action="{{ path('app_logout') }}" method="post" class="text-gray-700 hover:text-gray-900">
                            <input type="hidden" name="_csrf_token" value="{{ csrf_token('logout') }}">
                            <button type="submit">Déconnexion</button>
                        </form>
                    </ul>
                </nav>
            </div>
        </header>

        <!-- Hero Section -->
        <section class="relative bg-cover bg-center h-64" style="background-image: url('{{ asset('images/restaurant.jpg') }}');">
            <div class="absolute inset-0 bg-black bg-opacity-50 flex items-center justify-center">
                <h2 class="text-white text-5xl font-bold">Découvrez une expérience culinaire unique</h2>
            </div>
        </section>

        <!-- Menu Section -->
        <section class="flex flex-col items-center justify-center my-10">
            <h2 class="text-3xl font-bold text-center text-gray-800 mb-6">Menus de la semaine</h2>

            <div id="menu-grid" class="grid grid-cols-1 md:grid-cols-7 gap-30px border border-gray-300 rounded-xl p-8 w-[1500px] bg-white shadow-2xl">
                <!-- Les cartes seront insérées ici dynamiquement -->
            </div>

            <!-- Bouton pour rafraîchir -->
            <div class="text-center mt-6">
                <button id="refresh-menu" class="bg-blue-500 text-white px-6 py-3 rounded-lg shadow-md hover:bg-blue-700 transition">
                    Changer la sélection
                </button>
            </div>
        </section>


        {% block footer %}
            <footer class="bg-gray-900 text-white py-6 text-center">
                <p>&copy; 2025 Le Gourmet. Tous droits réservés.</p>
            </footer>
        {% endblock %}
    </div>

    <script src="{{ asset('js/menu.js') }}"></script>
{% endblock %}
